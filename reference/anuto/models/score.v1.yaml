title: Score
type: object
description: This entity represents a result of a gameplay by a player in a tournament
properties:
  id:
    type: string
    description: Id of score
  score:
    type: integer
    description: 'Score of the player in the tournament, if already revealed'
  log:
    $ref: ./log.v1.yaml
required:
  - id
