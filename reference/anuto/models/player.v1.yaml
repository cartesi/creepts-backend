title: Player
type: object
description: This entity represents a play of a player in a tournament
properties:
  id:
    type: string
    description: Id of player
  score:
    type: integer
    description: 'Score of player in a tournament, if already revealed'
  log:
    $ref: ./log.v1.yaml
required:
  - id
